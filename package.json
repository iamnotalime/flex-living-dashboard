{
  "name": "flex-insights-full-stack",
  "version": "1.0.0",
  "description": "Monorepo for Flex Insights Dashboard (React + Node/Express Mock API)",
  "scripts": {
    "build": "npm run build --prefix client",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}